spring:
  application:
    name: cheese-ai-code-backend
  # mysql数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cheese_ai_code
    username: root
    password: 5201314WZH
 #主配置文件中激活本地配置
  profiles:
    active: local
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.ustinian.cheeseaicode.controller
# knife4j
knife4j:
  enable: true
  setting:
    language: zh_cn

##本地local的配置文件
## AI模型配置
#langchain4j:
#  open-ai:
#    # 普通聊天模型配置
#    chat-model:
#      base-url: https://api.deepseek.com
#      api-key: REDACTED_API_KEY
#      model-name: deepseek-chat
#      max-tokens: 8192
#      # 支持结构化输出
#      strict-json-schema: true
#      response-format: json_object
#      log-requests: true
#      log-responses: true
#
#    # 流式聊天模型配置
#    streaming-chat-model:
#      base-url: https://api.deepseek.com
#      api-key:
REDACTED_API_KEY      model-name: deepseek-chat
#      max-tokens: 8192
#      # 流式响应不支持结构化输出
#      # strict-json-schema: true
#      # response-format: json_object
#      log-requests: true
#      log-responses: true